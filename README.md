# Multilingual Story Generation with Planning project

### Downloading relevant stories

To load the relevant stories for every language, run the `process_data` script as follows, e.g.:

```
bash process_data de
```
